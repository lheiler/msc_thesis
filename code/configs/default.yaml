#choose method from: 
#ctm_nn_avg, ctm_nn_pc, 
#pca_avg, pca_pc,
#eegnet,
#c22 
#jr_avg, jr_pc, 
#wong_wang_avg, wong_wang_pc, 
#hopf_avg, hopf_pc, 
#psd_ae_avg, psd_ae_pc, 
#psd_ae_channel, psd_ae_ch
#ctm_cma_pc, ctm_cma_avg

method: c22

# Ensure datasets are specified correctly for evaluation
datasets:
  lemon: "/Users/lorenzheiler/msc_thesis/Datasets/lemon"
  tuh: "/Users/lorenzheiler/msc_thesis/Datasets/tuh-eeg-ab-clean"

paths:
  results_root: "Results"

# ----------------------------------------------------------------------------
# Hyper-parameter optimisation via Optuna (overrides manual settings)
optuna:
  n_trials: 50  # Number of optimisation trials per task
  val_split: 0.15   # Fraction of training data reserved for validation
  patience: 10   # Early-stopping patience within each trial
  batch_size: 256
